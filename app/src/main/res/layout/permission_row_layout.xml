<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/ivPermission"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="5dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:src="@drawable/permission_icon"/>
    <TextView
        android:id="@+id/tvLabel"
        style="@style/ListTitleStyle.Dispatch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/title_text_size"
        android:layout_marginLeft="@dimen/_14ssp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toRightOf="@+id/ivPermission"
        android:text="Access Camera" />

    <TextView
        android:id="@+id/tvDescription"
        style="@style/list_subtitle_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@+id/tvLabel"
        android:layout_marginTop="@dimen/_4sdp"
        app:layout_constraintEnd_toEndOf="parent"
        android:textSize="@dimen/title_text_size"
        app:layout_constraintTop_toBottomOf="@+id/tvLabel"
        android:text="Allow to access the camera app to acess the phone features of the devices. This permission allows the app to determine the phone number and device ids, whether a call is active, and the remote number connected by a call." />

    <View
        style="@style/SettingSeprator"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginEnd="0dp"
        app:layout_constraintTop_toBottomOf="@+id/tvDescription"
        android:layout_marginTop="@dimen/_12sdp"
        android:layout_marginBottom="@dimen/_24sdp"/>
</androidx.constraintlayout.widget.ConstraintLayout>